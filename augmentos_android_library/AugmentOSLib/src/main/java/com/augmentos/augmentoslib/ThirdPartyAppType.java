package com.augmentos.augmentoslib;

public enum ThirdPartyAppType {
    CORE_SYSTEM, // Unkillable, invisible, full access to system resources
    APP, // Typical permissions, interaction
    DASHBOARD // Implementation undecided
}